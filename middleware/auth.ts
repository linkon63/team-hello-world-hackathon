// middleware/checkEmail.js
// export default function ({ event }) {
//     console.log('beelo')
//     // Check if the email exists in sessionStorage
//     if (!process.client) return; // Ensure this runs only on the client side
  
//     const email = sessionStorage.getItem('email');
//     if (!email) {
//       // Redirect to the index page if email is not found
//     //   return redirect('/');
//     }
//   }
  